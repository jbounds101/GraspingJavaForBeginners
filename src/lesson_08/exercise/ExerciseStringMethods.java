package lesson_08.exercise;

public class ExerciseStringMethods {
    public static String formalize(String toModify) {
        /* TODO: Finish this method
        This method should:
        1. Immediately return the toModify string if it contains a '?' or '!'
        2. Else, make the entire string lowercase, except for the character at index 0 (which should be ignored)
         */
        return null;
    }
}
